<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.appgl.dao.datadictionaryDao">
	<select id="getById" parameterType="int" resultType="datadictionary">
		select * from data_dictionary where id = #{id}
	</select>
	<select id="getAllt" parameterType="string" resultType="datadictionary">
		select * from data_dictionary where typeCode = #{entity}
	</select>
</mapper>